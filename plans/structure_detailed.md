# 🏗️ هيكل نظام EDUCORE المفصل | Detailed System Structure

## 📐 البنية المعمارية | Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          EDUCORE V2 - نظام إدارة تعليمي                      │
├─────────────────────────────────────────────────────────────────────────────┤
│  Frontend (MVC JavaScript)    │    Backend (Django REST Framework)          │
│  ┌─────────────────────────┐  │    ┌─────────────────────────────────────┐  │
│  │ Views: ScannerView      │  │    │ Apps: 7 Django Apps                 │  │
│  │        DashboardView    │  │    │ ├─ accounts (User Management)       │  │
│  │        ReportView       │  │    │ ├─ students (Student Management)    │  │
│  │ Controllers:            │  │    │ ├─ teachers (Teacher/Group/Room)    │  │
│  │   ScannerController     │  │    │ ├─ attendance (Session/Attendance)  │  │
│  │   StudentController     │  │    │ ├─ payments (Payment/Settlement)    │  │
│  │   ReportController      │  │    │ ├─ notifications (SMS/WhatsApp)     │  │
│  │ Models: Student,        │  │    │ └─ reports (PDF/Excel Export)      │  │
│  │         Attendance,     │  │    └─────────────────────────────────────┘  │
│  │         Payment         │  │    Background Tasks: Celery + Redis        │
│  │ Utils: API Client       │  │    Database: PostgreSQL / SQLite           │
│  └─────────────────────────┘  └─────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🎯 الميزات الرئيسية | Core Features

### 1️⃣ نظام الحضور الذكي | Smart Attendance System
```
┌─────────────────────────────────────────────────────────────────┐
│  AttendanceService - خوارزمية الفحص الثلاثي (Triple Check)      │
├─────────────────────────────────────────────────────────────────┤
│  Step 1: التعريف | Identification                               │
│    ├── جلب الطالب بـ student_code                               │
│    └── التحقق من is_active                                      │
│                                                                 │
│  Step 2: مطابقة الجدول | Schedule Matching                      │
│    ├── فحص اليوم (schedule_day)                                 │
│    └── جلب المجموعات المسجل فيها الطالب                        │
│                                                                 │
│  Step 3: قاعدة الـ 10 دقائق الصارمة | Time Check                │
│    ├── الوصول المبكر (+30 دقيقة مسموح)                         │
│    ├── فترة السماح (10 دقائق) = متأخر                          │
│    ├── تأخير أكثر من 10 دقائق = ممنوع                          │
│    └── بعد انتهاء الحصة = ممنوع                                │
│                                                                 │
│  Step 4: الفحص المالي | Financial Status Check                  │
│    ├── عادي (normal) - مسموح                                   │
│    ├── رمزي (symbolic) - مسموح                                 │
│    ├── إعفاء (exempt) - مسموح                                  │
│    └── غير مدفوع (unpaid) - ممنوع                              │
└─────────────────────────────────────────────────────────────────┘
```

### 2️⃣ نظام المدفوعات | Payment System
```
┌─────────────────────────────────────────────────────────────────┐
│  Payment Models & Services                                      │
├─────────────────────────────────────────────────────────────────┤
│  StudentGroupEnrollment (الحالة المالية لكل مجموعة)           │
│  ├── financial_status: normal | symbolic | exempt              │
│  └── custom_fee: مبلغ مخصص للحالة الرمزية                      │
│                                                                 │
│  Payment Model (سجل المدفوعات)                                 │
│  ├── amount_due: المبلغ المطلوب                                │
│  ├── amount_paid: المبلغ المدفوع                               │
│  ├── status: paid | partial | unpaid                           │
│  └── unique_together: [student, group, month]                  │
│                                                                 │
│  SettlementService (حساب مستحقات المدرسين)                     │
│  ├── calculate_teacher_settlement()                            │
│  ├── calculate_group_revenue()                                 │
│  └── center_percentage: نسبة المركز (30%)                      │
└─────────────────────────────────────────────────────────────────┘
```

### 3️⃣ نظام الإشعارات | Notification System
```
┌─────────────────────────────────────────────────────────────────┐
│  Notification Services (Celery + WhatsApp/SMS)                 │
├─────────────────────────────────────────────────────────────────┤
│  WhatsAppService (UltraMsg API)                                │
│  ├── send_message() - إرسال رسالة واتساب                       │
│  └── _format_phone_number() - تنسيق رقم الهاتف                 │
│                                                                 │
│  NotificationLog (سجل الإشعارات)                               │
│  ├── notification_type: attendance | late | absent |           │
│  │                    payment_reminder | payment_warning       │
│  ├── status: sent | failed | pending                           │
│  └── message, error_message, sent_at                           │
│                                                                 │
│  Celery Tasks (معالجة في الخلفية)                              │
│  ├── send_attendance_notifications()                           │
│  ├── send_payment_reminders()                                  │
│  └── process_pending_notifications()                           │
└─────────────────────────────────────────────────────────────────┘
```

### 4️⃣ نماذج البيانات | Data Models
```
┌─────────────────────────────────────────────────────────────────┐
│  Core Models                                                    │
├─────────────────────────────────────────────────────────────────┤
│  User (accounts) - Custom User Model                           │
│  ├── role: admin | supervisor | teacher                        │
│  ├── phone, is_active, created_at                              │
│  └── methods: is_admin(), is_supervisor()                      │
│                                                                 │
│  Student (students)                                             │
│  ├── student_code (unique, indexed)                            │
│  ├── full_name, parent_phone                                   │
│  └── groups (ManyToMany through StudentGroupEnrollment)        │
│                                                                 │
│  Teacher, Group, Room (teachers)                               │
│  ├── Teacher: full_name, specialization, hire_date             │
│  ├── Group: teacher, room, schedule_day, schedule_time,        │
│  │         standard_fee, center_percentage                     │
│  └── Room: name, capacity                                      │
│                                                                 │
│  Session, Attendance (attendance)                              │
│  ├── Session: group, session_date, teacher_attended,           │
│  │          is_cancelled, notification_sent                    │
│  └── Attendance: student, session, status, scan_time           │
└─────────────────────────────────────────────────────────────────┘
```

### 5️⃣ الواجهات الأمامية | Frontend Structure (MVC)
```
┌─────────────────────────────────────────────────────────────────┐
│  JavaScript MVC Architecture                                   │
├─────────────────────────────────────────────────────────────────┤
│  Models (static/js/models/)                                    │
│  ├── Student.js - نموذج الطالب                                │
│  ├── Attendance.js - نموذج الحضور                             │
│  └── Payment.js - نموذج المدفوعات                             │
│                                                                 │
│  Views (static/js/views/)                                      │
│  ├── ScannerView.js - واجهة الماسح الضوئي                     │
│  ├── DashboardView.js - واجهة لوحة التحكم                     │
│  └── ReportView.js - واجهة التقارير                           │
│                                                                 │
│  Controllers (static/js/controllers/)                          │
│  ├── ScannerController.js - التحكم في المسح                   │
│  ├── StudentController.js - التحكم في بيانات الطلاب           │
│  └── ReportController.js - التحكم في التقارير                 │
│                                                                 │
│  Utils (static/js/utils/)                                      │
│  └── api.js - عميل API للتواصل مع الخادم                      │
└─────────────────────────────────────────────────────────────────┘
```

### 6️⃣ التقنيات والأدوات | Technologies Stack
```
┌─────────────────────────────────────────────────────────────────┐
│  Tech Stack                                                     │
├─────────────────────────────────────────────────────────────────┤
│  Backend: Django 5.x + DRF + Celery + Redis                    │
│  Frontend: Vanilla JS (MVC) + Bootstrap 5.3 + RTL Support      │
│  Database: PostgreSQL (Production) / SQLite (Dev)              │
│  Notifications: UltraMsg (WhatsApp) / Twilio (SMS)             │
│  Reports: reportlab (PDF) / openpyxl (Excel)                   │
│  Deployment: Docker + Nginx + Gunicorn                         │
│  CI/CD: GitHub Actions (5 workflows)                           │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 إحصائيات المشروع | Project Statistics
```
┌─────────────────────────────────────────────────────────────────┐
│  Files Count                                                    │
├─────────────────────────────────────────────────────────────────┤
│  Django Apps: 7 (accounts, students, teachers, attendance,     │
│                 payments, notifications, reports)               │
│  Models: 10+ (User, Student, Teacher, Group, Room, Session,    │
│             Attendance, Payment, NotificationLog, etc.)         │
│  API Endpoints: 15+ (REST API)                                 │
│  JavaScript Files: 10+ (MVC Pattern)                           │
│  Documentation: 14+ MD Files                                   │
│  CI/CD Workflows: 5 GitHub Actions                             │
└─────────────────────────────────────────────────────────────────┘
```
