# Django
Django==5.0.1
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-filter==23.5

# Database
psycopg[binary]==3.2.13
redis==5.0.1
django-redis==5.4.0

# Celery for background tasks
celery==5.3.6
django-celery-beat==2.6.0

# WhatsApp
requests==2.31.0

# Security
django-ratelimit==4.1.0
python-decouple==3.8

# Timezone data
# TZ-09: apps/attendance/services.py imports pytz directly. It used to resolve
# only as a transitive dependency; pin it explicitly so the scanner cannot break
# with an ImportError when that transitive edge disappears.
pytz==2025.2

# Other utilities
Pillow==10.4.0
python-barcode==0.15.1
qrcode==7.4.2
reportlab==4.0.9
openpyxl==3.1.2
arabic-reshaper==3.0.0
python-bidi==0.6.7

# Production static files
whitenoise==6.6.0

# Production server
gunicorn==21.2.0

# SEC-15: django-debug-toolbar was listed here but is never added to
# INSTALLED_APPS or MIDDLEWARE. It was dead weight in the production image and
# has been removed. If it is ever needed, add it to a separate
# requirements-dev.txt together with the matching settings wiring.
