# إدارة المواد الدراسية (Subjects Management)

## نظرة عامة

تم إضافة نظام إدارة المواد الدراسية إلى نظام الحضور والمدفوعات. يمكنك الآن:
- إضافة مواد دراسية جديدة
- تعديل بيانات المواد الموجودة
- عرض تفاصيل المادة والمدرسين المتخصصين بها
- حذف المواد الدراسية

## الميزات الرئيسية

### 1. قائمة المواد الدراسية
- عرض جميع المواد الدراسية
- عرض عدد المدرسين لكل مادة
- سهولة البحث والتنقل
- بث الإحصائيات (إجمالي المواد)

### 2. إضافة مادة جديدة
- نموذج بسيط وسهل الاستخدام
- التحقق من عدم تكرار اسم المادة
- رسائل نجاح وتنبيهات واضحة

### 3. عرض تفاصيل المادة
- اسم المادة والمعلومات الأساسية
- قائمة المدرسين المتخصصين
- عدد المدرسين لكل مادة
- روابط سريعة إلى بيانات المدرس

### 4. تعديل المادة
- تحديث اسم المادة
- التحقق من عدم التكرار
- تتبع التعديلات

### 5. حذف المادة
- تأكيد حذف آمن
- منع الحذف العرضي
- رسائل تأكيد واضحة

## البنية التكنولوجية

### Models
```python
class Subject(models.Model):
    name = models.CharField(max_length=100, unique=True, verbose_name="اسم المادة")
    
    class Meta:
        db_table = 'subjects'
        verbose_name = 'مادة دراسية'
        verbose_name_plural = 'المواد الدراسية'
```

### Views (في apps/teachers/views.py)
- `subject_list`: عرض قائمة المواد مع إحصائيات
- `subject_create`: إضافة مادة جديدة
- `subject_detail`: عرض تفاصيل المادة والمدرسين
- `subject_update`: تعديل المادة
- `subject_delete`: حذف المادة مع تأكيد

### Forms (في apps/teachers/forms.py)
```python
class SubjectForm(forms.ModelForm):
    # التحقق من عدم تكرار الاسم
    # واجهة سهلة الاستخدام
```

### URLs (في apps/teachers/urls.py)
- `/teachers/subjects/` - قائمة المواد
- `/teachers/subjects/create/` - إضافة مادة
- `/teachers/subjects/<id>/` - تفاصيل المادة
- `/teachers/subjects/<id>/edit/` - تعديل المادة
- `/teachers/subjects/<id>/delete/` - حذف المادة

### Templates
- `templates/teachers/subjects/list.html` - قائمة المواد
- `templates/teachers/subjects/form.html` - نموذج الإضافة/التعديل
- `templates/teachers/subjects/detail.html` - تفاصيل المادة
- `templates/teachers/subjects/confirm_delete.html` - تأكيد الحذف

## التكامل مع النظام

### في نموذج Teacher
```python
subjects = models.ManyToManyField(
    Subject,
    blank=True,
    related_name='teachers',
    verbose_name="التخصصات / المواد"
)
```

### في نموذج Teacher Form
```python
subjects = forms.ModelMultipleChoiceField(
    queryset=Subject.objects.all(),
    required=False,
    widget=forms.CheckboxSelectMultiple(),
    label="التخصصات / المواد"
)
```

## الواجهة الجانبية (Sidebar)

تم إضافة رابط "المواد الدراسية" في الشريط الجانبي، ضمن قسم "إدارة البيانات":
```
📚 المواد الدراسية
```

يظهر الرابط وسط الخيارات الأخرى:
- الطلاب
- المدرسين
- **المواد الدراسية** ← جديد
- القاعات
- المجموعات
- المدفوعات

## أمثلة الاستخدام

### إضافة مادة دراسية
1. اضغط على "المواد الدراسية" في الشريط الجانبي
2. اضغط على "إضافة مادة جديدة"
3. أدخل اسم المادة (مثال: الرياضيات)
4. اضغط "إضافة"

### تعيين مادة إلى مدرس
1. ادهب إلى قسم "تعديل مدرس"
2. اختر المواد الدراسية من قائمة الاختيار
3. احفظ الترقيات

### عرض المدرسين لمادة معينة
1. ادهب إلى المواد الدراسية
2. اضغط على المادة المطلوبة
3. سترى جميع المدرسين المتخصصين

## الأذونات والصلاحيات

جميع العمليات تتطلب تسجيل الدخول:
```python
@login_required
def subject_list(request):
    ...
```

## رسائل الإجراءات

- ✅ "تم إضافة المادة الدراسية بنجاح"
- ✅ "تم تحديث بيانات المادة الدراسية بنجاح"
- ✅ "تم حذف المادة (اسم المادة) بنجاح"

## البيانات والإحصائيات

الصفحة الرئيسية للمواد تعرض:
- إجمالي عدد المواد الدراسية
- عدد المدرسين لكل مادة
- حالة كل مادة (نشطة/غير نشطة)

## الرد على الأخطاء

### التحقق من الصحة
- التحقق من عدم تكرار اسم المادة
- عدم السماح بإضافة مادة بدون اسم
- عدم السماح بحذف مادة قيد الاستخدام

### رسائل الخطأ
```
"هذه المادة موجودة بالفعل"
"حقل مطلوب"
```

## التطوير المستقبلي

يمكن إضافة الميزات التالية مستقبلاً:
- تصنيفات المواد (تخصصات فرعية)
- مستويات المواد (أساسي، متوسط، متقدم)
- ربط المواد بالمجموعات
- تقارير تحليلية للمواد الشعبية والمتخصصين
- استيراد/تصدير المواد

## الدعم والمساعدة

إذا واجهت أي مشاكل:
1. تأكد من تسجيل الدخول
2. تحقق من الأذونات
3. تأكد من إدخال البيانات بشكل صحيح
4. تحقق من سجل الأخطاء في Django

---

**آخر تحديث:** 2026-02-07
