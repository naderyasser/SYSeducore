# SYSeducore — نظام إدارة المراكز التعليمية

> نظام شامل لإدارة الطلاب، المجموعات، الحضور، المدفوعات، والطباعة — مبني على Django 5.0

---

## جدول المحتويات

1. [نظام الطلاب](#1-نظام-الطلاب)
2. [نظام الباركود والـ QR Code](#2-نظام-الباركود-والـ-qr-code)
3. [نظام الطباعة](#3-نظام-الطباعة)
4. [نظام الحضور والمسح](#4-نظام-الحضور-والمسح)
5. [نظام المجموعات والمدرسين](#5-نظام-المجموعات-والمدرسين)
6. [نظام الاشتراكات والمدفوعات](#6-نظام-الاشتراكات-والمدفوعات)
7. [Workflow كامل — من التسجيل للطباعة للحضور](#7-workflow-كامل)
8. [الـ API Endpoints](#8-الـ-api-endpoints)
9. [البنية التقنية](#9-البنية-التقنية)

---

## 1. نظام الطلاب

### بيانات الطالب

| الحقل | الوصف |
|-------|-------|
| `student_code` | كود تلقائي فريد (1001, 1002, ...) — يتولد بشكل thread-safe |
| `full_name` | اسم الطالب الكامل |
| `gender` | ذكر / أنثى |
| `education_stage` | المرحلة: ابتدائي / إعدادي / ثانوي |
| `education_year` | السنة الدراسية (1–6) |
| `education_type` | النوع: عام / لغات / تجريبي |
| `school_name` | اسم المدرسة |
| `student_phone` | تليفون الطالب |
| `parent_phone` | تليفون ولي الأمر (مطلوب) |
| `parent_name` | اسم ولي الأمر |
| `address` | العنوان |
| `subscription_expiry_date` | تاريخ انتهاء الاشتراك |
| `is_active` | نشط / غير نشط |

### توليد كود الطالب

```
generate_next_code() → يأخذ أعلى كود موجود + 1
- أول طالب يبدأ من 1001
- التوليد atomic (thread-safe) لمنع التكرار
- الكود فريد ومفهرس في قاعدة البيانات
```

### الـ Workflow:

```
إنشاء طالب → توليد كود تلقائي → توليد باركود Code128 → حفظ صورة الباركود
    ↓
إضافة لمجموعة/مجموعات مع تحديد الحالة المالية
    ↓
الطالب جاهز للحضور والطباعة
```

---

## 2. نظام الباركود والـ QR Code

### نوعين من الأكواد

| النوع | المكتبة | الاستخدام |
|-------|---------|-----------|
| **Code128 (1D)** | `python-barcode` + `ImageWriter` | الاستخدام الأساسي — في الجداول، البطاقات، الطباعة |
| **QR Code (2D)** | `qrcode` + `PIL` | احتياطي — غير مستخدم حالياً في الواجهة |

### إعدادات الباركود Code128

```python
# apps/students/models.py → generate_barcode_image()
Code128(student_code, writer=ImageWriter())
options = {
    'module_height': 15,      # ارتفاع الأعمدة
    'module_width': 0.6,      # عرض كل عمود
    'quiet_zone': 2,          # المساحة الفارغة حول الباركود
    'font_size': 1,           # حجم الخط (أقل قيمة ممكنة)
    'text_distance': 1,       # المسافة بين الباركود والنص
    'dpi': 300,               # جودة عالية للطباعة
    'write_text': False,      # عدم كتابة النص داخل الصورة
}
```

### كيف يتم تمرير الباركود للتمبلت

```python
# في الـ View:
barcode_base64 = student.get_barcode_base64()  # → PNG مشفر base64

# في الـ Template:
<img src="data:image/png;base64,{{ barcode_base64 }}">
```

### تخزين الباركود

```
media/barcodes/{student_code}.png   ← ملف PNG مخزن على السيرفر
+ base64 string                     ← يتولد في كل طلب للـ template
```

### الـ API

```
GET  /students/api/{id}/barcode/       → JSON: { success, barcode: "data:image/png;base64,..." }
POST /students/api/{id}/barcode/send/  → إرسال الباركود على واتساب ولي الأمر
GET  /students/api/{id}/whatsapp/      → رابط مشاركة واتساب + صورة الباركود
```

### المودال (تكبير الباركود)

في صفحة قائمة الطلاب:
```
الجدول يعرض thumbnail للباركود ← عند الضغط عليه يفتح مودال
المودال يجيب الباركود من API ← /students/api/{id}/barcode/
المودال يعرض: صورة الباركود مكبرة + اسم الطالب + كود الطالب
أزرار: تحميل الصورة | طباعة | إرسال واتساب
```

---

## 3. نظام الطباعة

### ثلاث صفحات طباعة

#### 1. تذكرة ستيكر — `qr_ticket.html`

**الوصف**: صفحة مزدوجة (كارت شاشة + ستيكر حراري)

**وضع الكارت (طباعة عادية)**:
- مقاس: 400mm × 100mm
- تصميم: كارت أزرق متدرج مع باركود أبيض
- المحتوى: اسم الطالب + كود الطالب + صورة الباركود
- خلفية: gradient من `#1e3a8a` → `#2563eb` → `#1e40af`

**وضع الستيكر الحراري (35mm × 10mm)**:
- مقاس الصفحة: `@page { size: 35mm 10mm; margin: 0 }` 
- المحتوى: **كود الطالب فقط** — بدون باركود، بدون اسم
- الخط: Cairo 8pt, bold 900, centered
- لا يوجد headers أو footers للمتصفح (margin: 0)

**الأزرار**:
| الزر | الوظيفة |
|------|---------|
| 🖨️ طباعة الكارت | يطبع الكارت الكبير 400mm×100mm |
| 🏷️ طباعة ستيكر | يطبع ستيكر 35mm×10mm (كود فقط) |
| ↩ رجوع | يرجع للصفحة السابقة |

**JavaScript**:
```javascript
printCard()    → body.classList.remove('thermal-mode') + @page 400mm 100mm
printThermal() → body.classList.add('thermal-mode')    + @page 35mm 10mm
setPageSize()  → ينشئ <style> ديناميكي مع @page rule
```

#### 2. بطاقة الطالب — `id_card.html`

**الوصف**: بطاقة هوية كاملة (تمتد من base.html)

- مقاس الطباعة: A4
- تصميم: كارت أزرق متدرج مع:
  - معلومات الطالب (اسم، كود، مرحلة، سنة)
  - صورة الباركود (300px عرض × 80px ارتفاع)
  - المجموعات المسجل فيها (حد أقصى 3)
- زر طباعة مدمج

#### 3. بطاقة طباعة مخصصة — `id_card_print.html`

**الوصف**: صفحة مستقلة (بدون base.html) مخصصة للطباعة

- مقاس: A4, centered
- تتضمن: Bootstrap 5 RTL + Cairo font
- الباركود: 280px عرض × 70px ارتفاع
- نفس معلومات id_card.html لكن بتصميم أبسط للطباعة

### ملخص أنواع الطباعة

```
┌─────────────────────┬──────────────┬────────────────────────────┐
│ الصفحة              │ المقاس       │ المحتوى                    │
├─────────────────────┼──────────────┼────────────────────────────┤
│ طباعة كارت          │ 400mm×100mm  │ كارت كامل + باركود        │
│ طباعة ستيكر         │ 35mm×10mm    │ كود الطالب فقط             │
│ بطاقة الطالب        │ A4           │ بطاقة هوية كاملة + باركود │
│ بطاقة طباعة مخصصة   │ A4           │ بطاقة للطباعة المباشرة     │
└─────────────────────┴──────────────┴────────────────────────────┘
```

---

## 4. نظام الحضور والمسح

### الـ Workflow

```
المشرف يفتح صفحة الماسح (/attendance/scanner/)
    ↓
يدخل كود الطالب (يدوي أو بقارئ باركود)
    ↓
النظام يعالج الكود عبر 5 خطوات:
```

### خوارزمية تسجيل الحضور (5 خطوات)

```
الخطوة 1: البحث عن الطالب
├── هل الكود موجود؟ ← لا → "كود غير صحيح"
├── هل الطالب نشط؟  ← لا → "الطالب غير نشط"
└── ✅ تم العثور على الطالب

الخطوة 2: فحص الاشتراك
├── هل الاشتراك نشط (30 يوم)؟ ← لا → "الاشتراك منتهي"
└── ✅ الاشتراك ساري

الخطوة 3: مطابقة الجدول
├── هل يوجد مجموعة مجدولة الآن؟ ← لا → "لا توجد حصة مجدولة"
├── المطابقة بناءً على: يوم الأسبوع + الوقت الحالي
└── ✅ تم تحديد المجموعة والحصة

الخطوة 4: فحص الوقت (صارم)
├── نافذة مبكرة: 30 دقيقة قبل الحصة ← ✅ مسموح
├── تأخير ≤ 10 دقائق ← ⚠️ "متأخر" (يسجل حضور)
├── تأخير > 10 دقائق ← ❌ ممنوع الدخول
└── ✅ / ⚠️ في الوقت المناسب

الخطوة 5: فحص مالي
├── معفى (exempt)؟ ← ✅ دخول مباشر
├── دفع الشهر الحالي؟ ← ✅ دخول
├── أول شهر في المجموعة؟ ← ✅ سماح مؤقت
├── متأخرات؟ ← ⚠️ تحذير (يسجل مع تنبيه)
└── ✅ مالياً مقبول
```

### النتائج الممكنة

| الحالة | الرسالة | الصوت |
|--------|---------|-------|
| ✅ حاضر | "تم تسجيل الحضور بنجاح" | success |
| ⚠️ متأخر | "تم التسجيل — متأخر" | warning |
| ❌ مرفوض | "ممنوع الدخول — [السبب]" | error |
| ❌ مكرر | "الطالب مسجل حضوره بالفعل" | info |

### نموذج الحضور

```python
class Attendance:
    student      → الطالب
    session      → الحصة
    scan_time    → وقت المسح
    status       → present / late / absent
    supervisor   → المشرف الذي سجل الحضور
    
class Session:
    group        → المجموعة
    session_date → تاريخ الحصة
    teacher_attended     → هل حضر المدرس
    teacher_checkin_time → وقت حضور المدرس
    is_cancelled         → ملغية؟
```

---

## 5. نظام المجموعات والمدرسين

### المدرس (Teacher)

```
teacher_id, full_name, phone, email
subjects    → علاقة M2M مع المواد
photo       → صورة المدرس (تظهر في خلف البطاقة)
hire_date   → تاريخ التعيين
```

### المجموعة (Group)

```
group_name          → اسم المجموعة
teacher             → المدرس المسؤول
room                → القاعة
schedule_day        → يوم المحاضرة
schedule_time       → وقت المحاضرة
duration_minutes    → المدة (افتراضي 120 دقيقة)
gender_type         → بنين / بنات / مختلط
education_stage     → المرحلة الدراسية
education_year      → السنة الدراسية
standard_fee        → الرسوم الشهرية
center_percentage   → نسبة المركز (افتراضي 30%)
sessions_per_month  → عدد الحصص في الشهر (افتراضي 4)
```

### جدول المجموعة (GroupSchedule)

```
group       → المجموعة
day_of_week → يوم الأسبوع (السبت–الجمعة)
start_time  → وقت البداية
duration    → المدة بالدقائق

← يمنع التعارض: نفس القاعة + نفس اليوم = الأوقات ما تتداخلش
```

### تسجيل الطالب في مجموعة (StudentGroupEnrollment)

```
student          → الطالب
group            → المجموعة
financial_status → الحالة المالية:
    ├── normal     → عادي (الرسوم الكاملة)
    ├── symbolic   → مبلغ رمزي (رسوم مخصصة)
    ├── exempt     → إعفاء كامل (مجاني)
    └── per_session → دفع بالحصة
custom_fee       → المبلغ المخصص (لو symbolic)
is_active        → نشط / غير نشط (soft delete)
```

---

## 6. نظام الاشتراكات والمدفوعات

### تفعيل الاشتراك

```
POST /students/api/{id}/subscription/activate/
    ↓
body: { days: 30 }  (أو 1, 2, 7, 30)
    ↓
student.last_payment_date = today
student.subscription_expiry_date = today + days
    ↓
الطالب يقدر يحضر لمدة {days} يوم من الآن
```

### فحص الاشتراك

```
GET /students/api/{id}/subscription/status/
    ↓
Response: {
    is_active: true/false,
    days_remaining: 15,
    expiry_date: "2026-05-01",
    status: "نشط" / "منتهي"
}
```

### الحالات المالية

```
┌──────────────┬────────────────────────────────────┐
│ الحالة       │ التأثير على الحضور                 │
├──────────────┼────────────────────────────────────┤
│ عادي         │ يجب دفع الرسوم الشهرية            │
│ مبلغ رمزي    │ يدفع مبلغ مخصص (custom_fee)       │
│ إعفاء كامل   │ دخول مجاني بدون فحص مالي          │
│ دفع بالحصة   │ يدفع عن كل حصة يحضرها            │
└──────────────┴────────────────────────────────────┘
```

---

## 7. Workflow كامل

### من التسجيل للطباعة للحضور

```
                    ┌──────────────────┐
                    │  إنشاء طالب جديد  │
                    └────────┬─────────┘
                             │
            ┌────────────────┼────────────────┐
            ↓                ↓                ↓
    توليد كود تلقائي   توليد باركود     حفظ البيانات
     (1001, 1002...)    Code128 PNG     في قاعدة البيانات
            │                │                │
            └────────────────┼────────────────┘
                             ↓
                    ┌──────────────────┐
                    │  إضافة لمجموعات   │
                    │  + حالة مالية     │
                    └────────┬─────────┘
                             │
            ┌────────────────┼────────────────┐
            ↓                ↓                ↓
      طباعة كارت       طباعة ستيكر      إرسال واتساب
     400mm×100mm       35mm×10mm       للباركود لولي الأمر
            │                │                │
            └────────────────┼────────────────┘
                             ↓
                    ┌──────────────────┐
                    │  تفعيل الاشتراك   │
                    │  (30 يوم)         │
                    └────────┬─────────┘
                             ↓
                    ┌──────────────────┐
                    │  الحضور اليومي    │
                    │  (مسح الباركود)   │
                    └────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        ↓                    ↓                    ↓
  فحص الاشتراك        مطابقة الجدول         فحص مالي
  (30 يوم نشط؟)       (حصة مجدولة الآن؟)   (دفع الشهر؟)
        │                    │                    │
        └────────────────────┼────────────────────┘
                             ↓
                    ┌──────────────────┐
                    │  تسجيل الحضور     │
                    │  present / late   │
                    └──────────────────┘
```

### دورة يوم عمل نموذجية

```
1. المشرف يسجل دخول → /accounts/login/
2. يفتح صفحة الماسح → /attendance/scanner/
3. الطالب يقدم الكود (يدوي أو بقارئ باركود)
4. النظام يتحقق: اشتراك ← جدول ← وقت ← مالي
5. ✅ يسجل الحضور أو ❌ يرفض مع السبب
6. المشرف يشوف إحصائيات اليوم: عدد الحاضرين، الحصص، إلخ
```

---

## 8. الـ API Endpoints

### الطلاب

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/students/` | قائمة الطلاب مع فلاتر |
| POST | `/students/create/` | إنشاء طالب |
| GET | `/students/{id}/` | تفاصيل الطالب |
| POST | `/students/{id}/update/` | تعديل الطالب |
| POST | `/students/{id}/delete/` | حذف (soft) |
| GET | `/students/{id}/qr-ticket/` | صفحة طباعة الستيكر/الكارت |
| GET | `/students/{id}/id-card/` | بطاقة الطالب |
| GET | `/students/{id}/id-card/print/` | بطاقة طباعة مخصصة |
| GET | `/students/api/{id}/barcode/` | API: باركود base64 |
| POST | `/students/api/{id}/barcode/send/` | إرسال باركود واتساب |
| POST | `/students/api/add-to-group/` | إضافة لمجموعة |
| POST | `/students/api/remove-from-group/` | إزالة من مجموعة |
| POST | `/students/api/{id}/subscription/activate/` | تفعيل اشتراك |
| GET | `/students/api/{id}/subscription/status/` | حالة الاشتراك |

### الحضور

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/attendance/scanner/` | صفحة الماسح |
| POST | `/attendance/api/process-code/` | معالجة كود الطالب |
| GET | `/attendance/api/today-stats/` | إحصائيات اليوم |
| GET | `/attendance/api/today-sessions/` | حصص اليوم |
| POST | `/attendance/api/teacher-checkin/{id}/` | تسجيل حضور المدرس |
| POST | `/attendance/api/cancel-session/{id}/` | إلغاء حصة |

---

## 9. البنية التقنية

### Stack

| المكون | التقنية |
|--------|---------|
| Backend | Django 5.0.1, Python 3.11+ |
| Database | SQLite (dev) |
| Server | Gunicorn 21.2.0 (4 gthread workers, 2 threads) |
| Proxy | Nginx (HTTPS via Let's Encrypt) |
| Barcode | `python-barcode` (Code128) + `Pillow` |
| QR Code | `qrcode` library |
| WhatsApp | UltraMsg API |
| Frontend | Bootstrap 5 RTL + Cairo font |
| Domain | https://sys.educore.software |

### هيكل الملفات

```
SYSeducore/
├── apps/
│   ├── accounts/     → المستخدمين والأدوار (admin/supervisor/teacher)
│   ├── students/     → الطلاب + الباركود + التسجيل في المجموعات
│   ├── teachers/     → المدرسين + المجموعات + القاعات + المواد + الجداول
│   ├── attendance/   → الحضور + الحصص + الماسح + سجل النشاط
│   ├── payments/     → المدفوعات والفواتير
│   ├── notifications/→ الإشعارات والواتساب
│   └── reports/      → التقارير والإحصائيات
├── config/           → إعدادات Django + URLs الرئيسية
├── templates/        → كل التمبلتس
│   └── students/
│       ├── list.html           → قائمة الطلاب + مودال الباركود
│       ├── form.html           → فورم إنشاء/تعديل الطالب
│       ├── detail.html         → تفاصيل الطالب
│       ├── qr_ticket.html      → طباعة كارت + ستيكر حراري
│       ├── id_card.html        → بطاقة هوية (ويب + طباعة)
│       └── id_card_print.html  → بطاقة طباعة مستقلة
├── static/           → CSS + JS + الأيقونات
├── media/
│   └── barcodes/     → صور الباركود المحفوظة
└── manage.py
```

### أدوار المستخدمين

| الدور | الصلاحيات |
|-------|-----------|
| `admin` | كل شيء — إدارة كاملة |
| `supervisor` | تسجيل الحضور + عرض الطلاب |
| `teacher` | عرض مجموعاته فقط |

### سجل النشاط (ActivityLog)

كل عملية في النظام بتتسجل مع:
- المستخدم الذي قام بها
- نوع العملية (student_create, attendance_scan, payment_record, ...)
- الوصف التفصيلي
- IP Address
- الوقت

---

## ملاحظات مهمة

1. **الحذف soft delete** — الطلاب والمدرسين والمجموعات ما بيتحذفوش نهائياً، بيروحوا سلة المحذوفات
2. **فحص التعارض** — النظام بيمنع تعارض القاعات (نفس القاعة + نفس اليوم + أوقات متداخلة)
3. **الباركود Code128** — يُولّد بدون نص أسفله (الكود يُعرض في HTML منفصل)
4. **الستيكر الحراري** — 35mm × 10mm، كود الطالب فقط، بدون headers/footers للمتصفح
5. **الاشتراك 30 يوم** — يُفعّل يدوياً، ويُفحص تلقائياً عند كل مسح حضور
